{
  "name": "pw_sample_project",
  "version": "1.0.0",
  "description": "Playwright Sample Project Testing UI &amp; API using POM &amp; Fixtures",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "test-ping": "npx playwright test --grep @ping --project=BasicProject",
    "test-negative": "npx playwright test --grep @negative --project=BasicProject"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.9.4",
    "dotenv": "^17.4.2",
    "monocart-reporter": "^2.10.0"
  }
}
